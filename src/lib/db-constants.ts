/**
 * Shared database constants
 * 
 * IMPORTANT: These constants are used by both the main thread and web workers.
 * Keep them in sync to avoid version mismatches.
 */

export const FILE_CONTENTS_DB_NAME = 'wiki-file-contents';
export const FILE_CONTENTS_DB_VERSION = 5;
